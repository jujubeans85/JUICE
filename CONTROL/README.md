# JUICE Control

Local-only control interface for the JUICE folders, pinned to Zed as the editor.

This repair makes Finder, Terminal, and Zed-launched sessions use the same durable Zed opener instead of relying on an interactive shell PATH.
