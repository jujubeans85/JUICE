JUICE READER — INSTALL ON THE WORKING MAC DASHBOARD

Work from ~/JUICE/CONTROL on the Mac. Preserve the repaired desktop first. Implement and test the integration; do not leave this as a proposal. You are authorized to make the necessary local changes, add the Reader module and event connection, and publish a focused branch. Preserve main and existing unrelated work.

The working Reader is https://juice-reader.xntfsstt5z.chatgpt.site. The downloadable juice-reader-jc-kit.zip contains the tested browser Reader, its shared intake rule, dashboard client and module contract. Obtain the kit from the user's Downloads/attachment, or through their existing authenticated Site access. Do not bypass the private Site's sign-in. If unavailable, request only the kit file, after completing all safe discovery.

1. Inspect the real Git root, branch, running application and current module/action registry. GitHub jujubeans85/JUICE still showed July state when this was prepared, so do not overwrite the working Mac with a remote checkout. Snapshot dirty/untracked source and relevant local configuration outside the repository, with checksums and recovery instructions, before editing.

2. Install Reader as one reusable module in the existing dashboard. Use its headphone icon, label READER and blue accent #82baff. Wire the main dashboard tile and global Read action through the existing action router. Do not build another dashboard or speech engine. Keep a per-module colour token in the existing registry so Reader is visibly blue; preserve other modules' styling unless the user has separately requested changes. Register name, url, health, current_item, pending_count and primary_action using the actual registry's schema, adapting module.json rather than blindly inserting it.

3. Serve the kit's reader/ static assets at /reader/ under JC's existing authenticated origin. The hosted Reader source is canonical; record this as a pinned release, not an independent fork. Preserve existing static-file safety and authentication. Do not expose a new public port or weaken pairing. Make /reader/ use the existing iOS/private-network entry point when available. The hosted URL remains a standalone fallback. These origins do not share browser memory; do not imply they synchronize automatically.

4. Load the SAME intake.js implementation in the dashboard, then integration/reader-client.js. Create exactly one JuiceReaderClient instance for the app shell. Bind the global Reader button directly to client.open() inside a user click. This reuses a named window. Handle blocked popups with a normal same-origin link. Do not auto-open windows for every reply.

5. Wire client.publish({id, conversationId, text, title, role, status}) into the actual assistant-final-response event already used by JC. Use stable, conversation-scoped message IDs. Supply only visible final assistant output: never reasoning, prompts, tool results, intermediate drafts or user messages. The shared rule accepts completed assistant replies with at least THREE non-empty blocks separated by blank lines, excludes heading-only blocks, and treats a fenced code block as one block. Two paragraphs do not qualify. Keep one implementation of this rule. The client queues until the reader connects, retries queued delivery on its readiness heartbeat, and requires an acknowledgement before calling a message delivered. Duplicates must not reload a reply. Loading is automatic; speaking waits for a user tap.

6. For cross-device delivery, reuse JC's existing authenticated event bus and canonical final-response records. Add a same-origin SSE adapter only if needed. Set window.JUICE_READER_CONFIG = {eventsUrl: '/the-actual-authenticated-reader-event-route'} before app.js executes. Send events named assistant.response.completed, with JSON matching {type:'juice.reader.response',version:1,id,conversationId,role:'assistant',status:'completed',text,title}. Reject unauthenticated access and unsafe cross-origin requests. Use existing message IDs/cursors for replay. Persist unread references to canonical messages instead of a second transcript database, and distinguish delivered from listened. Show pending replies without replacing text being read or manually edited. The included client-only queue is memory-only; use the canonical store for survival across browser restarts.

7. The reader now exposes two direct ChatGPT site tools via site-tools.js: juice_reader_status and juice_reader_receive_response. Preserve this file and its script order. In a supported ChatGPT desktop built-in browser, the agent can send its own completed reply to the open reader through the same intake rule and receive a loaded/queued receipt. Opening the page or registering tools does not prove delivery. Verify an actual status call and three-paragraph test delivery. The current official guide (https://learn.chatgpt.com/docs/webmcp) names Sol/Terra and desktop; iframe tools and Enterprise/Edu are unsupported. Native ChatGPT iPhone/iPad conversations are NOT an accessible event source for this kit. Do not claim they are connected and do not substitute clipboard watching or a fabricated polling feed. Inspect any legitimately configured Codex completion hooks, app-server events or other supported response-emitter integration and wire those if available, preserving existing hooks. Scope and label the actual connected source. If the JC GPT action only opens the ChatGPT website, report that an output connection is still missing; a launch button is not a conversation stream. Replies in the native mobile app remain manual paste or explicit #read= links until a supported connector exists.

8. Preserve voice and speed controls. Quick choices must represent distinct, real voice families exposed by the device, with sample playback and an honest count. Prefer enhanced/premium voices when available. Do not disguise one voice as several by changing pitch. Inspect installed Mac voices if useful. If extra voices must be downloaded, provide the exact OS settings action; do not pretend they were installed. Keep any existing authorized natural-voice provider server-side if you find one; do not expose keys or silently start a paid provider. A Mac's downloaded voices do not automatically appear on an iPhone.

9. Run focused checks: two paragraphs ignored; three accepted; headings do not inflate the count; fenced-code blank lines do not inflate it; user/tool/unfinished messages ignored; duplicate IDs suppressed; active reading/manual edits preserved when a new reply arrives; pending replies stay ordered; disabled intake acknowledged as disabled; disconnect/reconnect resumes delivery; a fresh authenticated browser opens the Reader tile; at least two actual voices sound different on a device that exposes them; volume/pause/speed still work. Do not mistake simulations for physical sound checks. If adding SSE, verify authorized replay and rejection without authentication, and name cross-device tests not run. Check existing dashboard smoke tests after the smallest relevant change; do not rerun unrelated suites without a concrete reason.

10. Preserve a rollback point and publish a small source-only integration branch using explicit staged paths. Scan every newly published commit for private data and secrets. Keep raw backups, voice caches, transcripts and runtime databases out of the public GitHub repo. Do not merge main automatically. Report the branch/commit, installed module route, source release, tests, exact rollback, and which conversation source is actually connected. Finish with INSTALLED AND VERIFIED or PREPARED — CONNECTION STILL MISSING, as supported by evidence. Never say this ChatGPT conversation streams to the reader unless a real end-to-end test proves it.
